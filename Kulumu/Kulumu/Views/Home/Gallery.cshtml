@using Kulumu
@using Kulumu.Models
@using SiteExtensions.Graphics
@model SiteModel 
@section Add
{
    @Html.Partial("Ad", new AdvertisingPanel { Article = Model.RandomArticle, DiscountProduct = Model.SingleDiscountProduct })
}
@section About
{
    @Html.Partial("About", new AboutModel { Title = Model.Content.DescriptionTitle, Text = Model.Content.Description })
}
<div class="mainFrame">
    <div class="top">
        @Html.Partial("Title", Model.Category.Title)
        <div class="description">
            @if (Request.IsAuthenticated)
            {
                @Html.ActionLink("Редактировать", "Edit", "Category", new { area = "Admin", id = Model.Category.Id }, new { @class = "adminLink" })
                <br />
            }
            @Model.Category.Description
        </div>
    </div>
    <div class="middle">
        <div class="products">
            @foreach (var product in Model.Products)
            {
                <div class="product">
                    <div class="cont">
                        <div class="image">
                            @Html.Raw(Html.CachedImage("~/Content/Images/", product.ImageSource, SiteSettings.GetThumbnail("galleryPreview"), ScaleMode.Crop))
                        </div>
                        <div class="frame">
                        </div>
                    </div>
                    @*<div class="description">
                        <div class="title">
                            
                        </div>
                    </div>*@
                </div>
            }
            <div class="clear">
            </div>
        </div>
    </div>
    <div class="bottom">
    </div>
</div>
