@model Filimonov.Models.ProductSet

@{
    ViewBag.Title = "AddProductSet";
    Layout = "~/Areas/Presentation/Views/Shared/_Layout.cshtml";
}

<h2>AddProductSet</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    
    @Html.Hidden("customerId", (string)ViewBag.CustomerId)
    
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>ProductSet</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Title)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Title)
            @Html.ValidationMessageFor(model => model.Title)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Details", new { id = (string)ViewBag.CustomerId })
</div>
