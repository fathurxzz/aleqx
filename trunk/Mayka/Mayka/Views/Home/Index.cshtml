@using Mayka.Helpers
@model Mayka.Models.SiteModel
@{
    
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (Request.IsAuthenticated && Model.Content.ContentType == (int)ContentType.HomePage)
{
    @Html.ActionLink("Редактировать главную страницу", "Edit", "Content", new { area = "Admin", id=Model.Content.Id }, new { @class = "adminLink" })
}

@Html.Partial("_ContentHeader",new ContentHeaderModel{Title = Model.Content.Title, Text = Model.Content.Text})

@section Menu
{
    @Html.Partial("Menu", Model.Menu)
}
